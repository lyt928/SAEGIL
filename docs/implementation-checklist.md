# 프로젝트 구현상황 체크리스트

## 상태 표시

- `[x]` 구현 완료
- `[~]` 일부 구현 또는 플레이스홀더만 존재
- `[ ]` 미구현 또는 확인 필요
- `검증 필요` 실행 또는 테스트 검증이 아직 필요함

## 1. 전체 구조

- `[x]` FastAPI 백엔드 진입점이 구성되어 있음
- `[x]` 기본 API 엔드포인트 `/`, `/health`가 존재함
- `[x]` 프레임 처리 파이프라인이 서비스 레이어로 분리되어 있음
- `[~]` 대시보드 앱 디렉터리는 있으나 실제 UI는 아직 플레이스홀더 수준임
- `[~]` 통합 테스트 디렉터리는 있으나 실제 테스트는 아직 비어 있음

## 2. 카메라 입력

- `[x]` 카메라 설정 구조체가 존재함
- `[~]` 카메라 소스, 해상도, FPS 메타정보를 다룰 수 있음
- `[ ]` OpenCV 기반 실제 카메라 프레임 수집 루프 구현
- `[ ]` USB/IP 카메라 전환 흐름 검증
- `[ ]` 실시간 스트림 안정성 및 FPS 측정

## 3. 객체 탐지

- `[x]` YOLO 탐지 래퍼 클래스가 구현되어 있음
- `[x]` 모델 경로 검증 로직이 있음
- `[x]` 신뢰도 임계값 필터링이 있음
- `[x]` 라벨 정규화 로직이 있음
- `[x]` 허용 라벨 필터링이 있음
- `[~]` 실제 YOLO 추론은 외부 패키지와 모델 파일에 의존함
- `[ ]` 실제 가중치 파일 존재 및 정상 로드 확인
- `[ ]` 샘플 프레임 대상 실제 추론 검증

## 4. PPE 인식

- `[x]` 사람, 안전모, 안전조끼 매칭 로직이 있음
- `[x]` 안전모 위치 판정 규칙이 있음
- `[x]` 안전조끼 위치 판정 규칙이 있음
- `[x]` 안전모 미착용 이벤트 생성 기반이 있음
- `[x]` 안전조끼 미착용 이벤트 생성 기반이 있음
- `[ ]` 실제 영상에서 다수 작업자 중첩 상황 검증
- `[ ]` 오탐률, 미탐률 측정

## 5. 위험구역 감지

- `[x]` 다각형 기반 위험구역 구조를 지원함
- `[x]` Point-in-Polygon 판정 로직이 있음
- `[x]` 작업자 발 위치 기준 침입 감지가 구현되어 있음
- `[x]` 위험구역 이벤트에 심각도, 구역 ID, 추적 ID가 포함됨
- `[ ]` 위험구역 편집 UI 또는 설정 워크플로우 확인
- `[ ]` 샘플 구역 파일이 실제 실행 흐름과 연결되어 있는지 확인

## 6. 위험 판정 및 경고

- `[x]` 규칙 기반 위험 판정 로직이 있음
- `[x]` 위험구역 침입 이벤트가 파이프라인 결과에 포함됨
- `[x]` PPE 미착용 이벤트가 생성됨
- `[x]` 경고 문구 생성 로직이 있음
- `[~]` 현재 위험 규칙은 PPE 중심의 기본 규칙 수준임
- `[ ]` 추락, 협착, 장비 근접, 자세 이상 등 확장 위험 규칙 구현
- `[ ]` 소리, 팝업, 외부 알림 연동 구현

## 7. 로그 및 데이터 관리

- `[x]` JSONL 기반 이벤트 로거가 구현되어 있음
- `[x]` 이벤트 ID와 UTC 타임스탬프가 자동 추가됨
- `[x]` 최근 이벤트 조회 기능이 있음
- `[x]` 로그 저장 경로가 설정으로 분리되어 있음
- `[ ]` SQLite 기반 로그 저장 구현
- `[ ]` 로그 보관 정책 문서화
- `[ ]` 이벤트 스키마 버전 관리 또는 마이그레이션 전략 정의

## 8. API 및 백엔드 완성도

- `[x]` 상태 확인용 응답 스키마가 있음
- `[~]` API 명세는 현재 기본 엔드포인트만 문서화되어 있음
- `[ ]` 추론 실행용 API 엔드포인트 구현
- `[ ]` 이벤트 목록 조회 API 구현
- `[ ]` 위험구역 등록, 수정, 삭제 API 구현
- `[ ]` WebSocket 또는 SSE 기반 실시간 이벤트 스트림 구현
- `[ ]` 예외 처리 및 응답 규칙 문서화

## 9. 대시보드 및 시연

- `[~]` 대시보드 폴더 구조만 존재함
- `[ ]` 실시간 영상 화면 구현
- `[ ]` 바운딩 박스 오버레이 구현
- `[ ]` 현재 경고 상태 패널 구현
- `[ ]` 최근 이벤트 목록 구현
- `[ ]` 데모 시나리오의 end-to-end 연결 확인
- `[ ]` 발표용 스크린샷 또는 시연 영상 준비

## 10. 테스트 준비도

- `[x]` PPE 로직 단위 테스트가 존재함
- `[x]` 위험구역 로직 단위 테스트가 존재함
- `[x]` 이벤트 로거 단위 테스트가 존재함
- `[x]` YOLO 파서 로직 단위 테스트가 존재함
- `[~]` 테스트 파일은 있으나 현재 환경에서 실행 검증은 못 함
- `[ ]` 로컬 환경에 `pytest` 설치
- `[ ]` 전체 단위 테스트 실행 성공 확인
- `[ ]` 통합 테스트 구현 및 실행

## 11. 배포 및 운영 준비

- `[x]` `.env.example` 파일이 존재함
- `[x]` `requirements.txt` 파일이 존재함
- `[~]` Jetson/엣지 배포 방향은 문서에 있으나 실제 검증은 안 됨
- `[ ]` 한 번에 실행 가능한 로컬 실행 절차 문서화 및 검증
- `[ ]` 운영용 설정 분리
- `[ ]` 모델 파일 다운로드 또는 설정 스크립트 준비
- `[ ]` 재시작, 장애 대응, 로그 운영 전략 정의

## 12. 당장 우선적으로 할 일

- `[ ]` 의존성 설치 후 단위 테스트 실행
- `[ ]` 실제 카메라 또는 샘플 영상 기반 추론 흐름 연결
- `[ ]` 이벤트 조회 API 추가
- `[ ]` 샘플 위험구역과 이벤트 로그를 포함한 실행 가능한 데모 구성
- `[ ]` 경고 상태와 최근 이벤트를 보여주는 최소 대시보드 구현
- `[ ]` 카메라 → 탐지 → 이벤트 생성까지 통합 테스트 추가

## 한줄 요약

현재 프로젝트는 백엔드 기본 구조와 PPE 판별, 위험구역 침입 감지, 이벤트 로그 저장까지는 구현이 진행된 상태입니다.

반면 실제 카메라 연동, 실추론 검증, 대시보드 구현, API 확장, 통합 테스트는 앞으로 보완이 필요한 단계입니다.
