# 프로젝트 구현상황 체크리스트

기준 시점: 현재 저장소 코드와 로컬 실행 결과 기준

## 상태 표시

- `[x]` 구현 완료 또는 로컬 검증 완료
- `[~]` 일부 구현, 수동 연결 필요, 또는 외부 의존성에 좌우됨
- `[ ]` 미구현
- `검증 필요` 코드/파일은 있으나 아직 실제 실행으로 확인하지 못함

## 1. 전체 구조

- `[x]` FastAPI 백엔드 진입점이 존재함
- `[x]` 기본 엔드포인트 `/`, `/health`가 동작함
- `[x]` 프레임 처리 파이프라인이 서비스 레이어로 분리되어 있음
- `[x]` 코어 모듈이 `camera`, `detection`, `ppe`, `zones`, `risk`, `alerts`, `logging`으로 분리되어 있음
- `[~]` 대시보드 디렉터리는 있으나 실제 UI 구현은 없음
- `[~]` 통합 테스트 디렉터리는 있으나 실제 테스트 케이스는 없음

## 2. 카메라 입력

- `[x]` 카메라 설정 구조체(`source`, `width`, `height`, `fps`)가 있음
- `[~]` 카메라 메타정보를 다루는 수준까지 구현됨
- `[ ]` OpenCV 기반 실시간 프레임 수집 루프
- `[ ]` USB/IP 카메라 전환 및 복수 소스 처리
- `[ ]` 실제 카메라 입력과 탐지 파이프라인 연결
- `[ ]` 스트림 안정성/FPS 측정

## 3. 객체 탐지

- `[x]` YOLO 탐지 래퍼 클래스가 구현되어 있음
- `[x]` 모델 경로 검증 로직이 있음
- `[x]` confidence threshold 필터링이 있음
- `[x]` 라벨 정규화 로직이 있음
- `[x]` 허용 라벨 필터링이 있음
- `[x]` 기본 모델 경로 상수와 경로 유틸이 있음
- `[x]` 모델 파일 `models/weights/stage1_best.pt` 존재 확인
- `[~]` 실제 YOLO 추론은 `ultralytics`와 런타임 환경에 의존함
- `[ ]` 샘플 이미지/영상 대상 실제 추론 검증

## 4. PPE 인식

- `[x]` 사람-안전모-안전조끼 매칭 로직이 있음
- `[x]` 안전모 위치 판정 규칙이 있음
- `[x]` 안전조끼 위치 판정 규칙이 있음
- `[x]` PPE 결과를 사람 단위로 반환함
- `[x]` PPE 미착용 이벤트 생성 기반이 있음
- `[ ]` 실제 영상에서 다수 작업자 겹침 상황 검증
- `[ ]` 오탐률/미탐률 측정

## 5. 위험구역 감지

- `[x]` 다각형 기반 위험구역 구조를 지원함
- `[x]` Point-in-Polygon 판정 로직이 있음
- `[x]` 작업자 발 위치 기준 침입 감지가 구현되어 있음
- `[x]` 위험구역 이벤트에 심각도, 구역 ID, 구역명, 추적 ID가 포함됨
- `[~]` 샘플 구역 데이터 파일은 존재함
- `[ ]` 위험구역 편집 UI 또는 관리 워크플로우
- `[ ]` 샘플 구역 파일과 실제 실행 흐름 연결

## 6. 위험 판정 및 경고

- `[x]` 규칙 기반 위험 판정 로직이 있음
- `[x]` PPE 미착용 이벤트가 생성됨
- `[x]` 위험구역 침입 이벤트가 생성됨
- `[x]` 경고 문구 생성 로직이 있음
- `[~]` 현재 위험 규칙은 PPE 및 구역 침입 중심의 기본 규칙 수준임
- `[ ]` 추락, 협착, 장비 근접, 자세 이상 등 확장 위험 규칙
- `[ ]` 외부 알림, 소리, 팝업 연동

## 7. 로그 및 데이터 관리

- `[x]` JSONL 기반 이벤트 로거가 구현되어 있음
- `[x]` 이벤트 ID와 UTC 타임스탬프가 자동 추가됨
- `[x]` 최근 이벤트 조회 기능이 있음
- `[x]` 로그 저장 경로가 설정으로 분리되어 있음
- `[ ]` SQLite 기반 로그 저장
- `[ ]` 로그 보관 정책 문서화
- `[ ]` 이벤트 스키마 버전 관리/마이그레이션 전략

## 8. API 및 백엔드

- `[x]` FastAPI 앱이 로컬에서 기동됨
- `[x]` `GET /` 응답 검증 완료
- `[x]` `GET /health` 응답 검증 완료
- `[x]` 상태 확인용 응답 스키마가 있음
- `[~]` 현재 API는 상태 확인용 최소 엔드포인트만 구현됨
- `[ ]` 추론 실행 API
- `[ ]` 이벤트 목록 조회 API
- `[ ]` 위험구역 등록/수정/삭제 API
- `[ ]` WebSocket 또는 SSE 기반 실시간 이벤트 스트림
- `[ ]` 예외 처리/응답 규칙 문서화

## 9. 대시보드 및 시연

- `[~]` 대시보드 폴더와 README만 존재함
- `[ ]` 실시간 영상 화면
- `[ ]` 바운딩 박스 오버레이
- `[ ]` 현재 경고 상태 패널
- `[ ]` 최근 이벤트 목록
- `[ ]` 데모 시나리오 end-to-end 연결
- `[ ]` 발표용 스크린샷/시연 영상 정리

## 10. 테스트

- `[x]` PPE 로직 단위 테스트가 있음
- `[x]` 위험구역 로직 단위 테스트가 있음
- `[x]` 이벤트 로거 단위 테스트가 있음
- `[x]` YOLO 파서 로직 단위 테스트가 있음
- `[x]` 테스트 보조용 `tests/conftest.py`가 있음
- `[x]` 로컬 환경에서 단위 테스트 `13 passed` 확인
- `[~]` 현재 테스트 범위는 단위 테스트 중심임
- `[ ]` 통합 테스트 구현
- `[ ]` 실제 모델 추론 포함 테스트
- `[ ]` 카메라 입력부터 이벤트 생성까지 end-to-end 테스트

## 11. 설정 및 배포 준비

- `[x]` `requirements.txt` 존재
- `[x]` 가상환경 기준 Python 3.12 실행 검증 완료
- `[x]` 모델 경로 설정값이 백엔드 설정에서 읽힘
- `[ ]` `.env.example` 복원 또는 설정 문서 정리
- `[ ]` 운영용 설정 분리
- `[ ]` 모델 다운로드/설정 스크립트 준비
- `[ ]` 로컬 실행 절차 문서 최신화
- `[ ]` Jetson/엣지 배포 실제 검증

## 12. 다음 우선순위 후보

- `[ ]` 실제 카메라 또는 샘플 영상과 `YoloDetector` 연결
- `[ ]` `process_frame`를 감싸는 추론 실행 API 추가
- `[ ]` 최근 이벤트 조회 API 추가
- `[ ]` 샘플 위험구역 + 이벤트 로그 기반 최소 데모 구성
- `[ ]` 경고 상태와 최근 이벤트를 보여주는 최소 대시보드 구현
- `[ ]` 통합 테스트 1개 이상 추가

## 한줄 요약

현재 프로젝트는 백엔드 최소 기동, PPE 판정, 위험구역 침입 감지, 이벤트 로그 저장, 단위 테스트까지는 동작합니다.

반면 실제 카메라 연동, 실추론 검증, 이벤트 조회 API, 대시보드, 통합 테스트는 다음 단계입니다.
