# 프로젝트 구현상황 체크리스트

## 상태 표시

- `[x]` 구현 완료
- `[~]` 부분 구현 또는 뼈대만 존재
- `[ ]` 미구현
- `검증 필요` 실행 또는 테스트 확인이 더 필요함

## 1. 전체 구조

- `[x]` FastAPI 백엔드 진입점이 구성되어 있음
- `[x]` 기본 API 엔드포인트 `/`, `/health`가 존재함
- `[x]` 프레임 처리 파이프라인이 서비스 레이어로 분리되어 있음
- `[x]` 모델 기본 경로가 `stage1_best.pt` 기준으로 통일되어 있음
- `[~]` 대시보드 디렉터리는 있으나 실제 UI 구현은 없음
- `[~]` 통합 테스트 디렉터리는 있으나 실제 테스트는 비어 있음

## 2. 카메라 입력

- `[x]` 카메라 설정 구조체가 존재함
- `[x]` CCTV 데모 스크립트가 카메라 또는 영상 파일 입력을 받을 수 있음
- `[x]` 웹캠/영상 입력 -> YOLO -> 파이프라인 연결용 데모 스크립트가 존재함
- `[~]` OpenCV 기반 프레임 루프는 데모 스크립트 수준으로 구현됨
- `[ ]` 백엔드 서버와 실시간 카메라 입력이 직접 연결되어 있지는 않음
- `[ ]` USB/IP 카메라 환경별 검증
- `[ ]` FPS 측정 및 성능 로그 정리

## 3. 객체 탐지

- `[x]` YOLO 탐지 래퍼 클래스가 구현되어 있음
- `[x]` 모델 경로 존재 여부 검증 로직이 있음
- `[x]` 신뢰도 임계값 필터링이 있음
- `[x]` 라벨 정규화 로직이 있음
- `[x]` 허용 라벨 필터링이 있음
- `[~]` 실제 YOLO 추론은 외부 패키지와 모델 파일에 의존함
- `[~]` 현재 실행 기준 클래스는 `person`, `helmet`, `vest` 중심임
- `[ ]` 실제 CCTV 화면 기준 추론 품질 검증

## 4. PPE 인식

- `[x]` 사람, 안전모, 안전조끼 매칭 로직이 있음
- `[x]` 안전모 위치 판정 규칙이 있음
- `[x]` 안전조끼 위치 판정 규칙이 있음
- `[x]` 안전모 미착용 이벤트 생성 기반이 있음
- `[x]` 안전조끼 미착용 이벤트 생성 기반이 있음
- `[ ]` 다수 작업자 밀집 환경 검증
- `[ ]` 실제 영상 기준 오탐/미탐 측정

## 5. 위험구역 감지

- `[x]` polygon 기반 위험구역 구조가 구현되어 있음
- `[x]` point-in-polygon 판정 로직이 있음
- `[x]` 사람 발 위치 기준 침입 감지가 구현되어 있음
- `[x]` 위험구역 이벤트에 구역 ID와 추적 ID가 포함됨
- `[x]` 샘플 구역 파일이 존재함
- `[ ]` 위험구역 편집 UI 또는 관리 API
- `[ ]` 다양한 현장 구역 템플릿 정리

## 6. 위험 판정 및 경고

- `[x]` 규칙 기반 위험 판정 로직이 있음
- `[x]` 위험구역 침입 이벤트가 파이프라인 결과에 포함됨
- `[x]` PPE 미착용 이벤트가 생성됨
- `[x]` 경고 문구 생성 로직이 있음
- `[~]` 현재 위험 규칙은 PPE와 zone 중심의 기본 규칙 수준임
- `[ ]` 체류 시간 기반 경고
- `[ ]` PPE 미착용 + 위험구역 진입 복합 경고
- `[ ]` 추락, 협착, 장비 근접 등 확장 위험 규칙 구현
- `[ ]` 이벤트 중복 억제 로직

## 7. 로그 및 데이터 관리

- `[x]` JSONL 기반 이벤트 로거가 구현되어 있음
- `[x]` 이벤트 ID와 UTC 타임스탬프가 자동 추가됨
- `[x]` 최근 이벤트 조회 기능이 있음
- `[x]` 로그 저장 경로가 설정으로 분리되어 있음
- `[ ]` SQLite 또는 DB 기반 로그 저장
- `[ ]` 로그 보존 정책 문서화
- `[ ]` 이벤트 스키마 버전 관리

## 8. API 및 백엔드 완성도

- `[x]` 상태 확인용 응답 스키마가 있음
- `[~]` API 명세는 기본 엔드포인트 위주로만 문서화됨
- `[ ]` 최근 이벤트 조회 API
- `[ ]` 전체 이벤트 목록 조회 API
- `[ ]` 이벤트 상세 조회 API
- `[ ]` 위험구역 등록/수정/삭제 API
- `[ ]` WebSocket 또는 SSE 기반 실시간 이벤트 스트림
- `[ ]` 예외 처리 규칙 정리

## 9. 대시보드 및 시연

- `[x]` CCTV 파이프라인 데모 스크립트가 존재함
- `[~]` 대시보드 폴더 구조만 존재함
- `[~]` 데모 화면에서 위험구역, 감지 결과, 경고 문구 표시 가능
- `[ ]` 실시간 영상 대시보드 UI
- `[ ]` 현재 경고 상태 패널
- `[ ]` 최근 이벤트 목록 UI
- `[ ]` 이벤트 상세 및 스냅샷/클립 확인 UI
- `[ ]` 발표용 end-to-end 시나리오 확정

## 10. 테스트 준비도

- `[x]` PPE 로직 단위 테스트가 존재함
- `[x]` 위험구역 로직 단위 테스트가 존재함
- `[x]` 이벤트 로거 단위 테스트가 존재함
- `[x]` YOLO 파서 로직 단위 테스트가 존재함
- `[~]` 테스트 파일은 있으나 전체 실행 결과는 별도 확인 필요
- `[ ]` 파이프라인 통합 테스트
- `[ ]` API 테스트
- `[ ]` 실제 모델 로드 포함 검증

## 11. 배포 및 운영 준비

- `[x]` `.env.example` 파일이 존재함
- `[x]` `requirements.txt` 파일이 존재함
- `[x]` `.gitignore`가 추가되어 가상환경/캐시 제외가 가능함
- `[~]` Jetson 또는 엣지 배포 방향은 문서 수준으로만 존재함
- `[ ]` 서버 실행 가이드 검증
- `[ ]` 운영 환경 설정 분리
- `[ ]` 모델 파일 배포 절차 정리
- `[ ]` 장애 대응 및 로그 운영 정책

## 12. 지금 우선적으로 할 일

- `[ ]` `cctv_pipeline_demo.py` 실제 실행 확인
- `[ ]` `stage1_best.pt` 추론 품질 확인
- `[ ]` 이벤트가 `data/processed/events.jsonl`에 정상 저장되는지 확인
- `[ ]` 이벤트 중복 저장 방지
- `[ ]` 위험구역 체류 시간 감지 추가
- `[ ]` `/events/recent` API 추가
- `[ ]` `/events` API 추가
- `[ ]` `/events/{id}` API 추가
- `[ ]` 최소 대시보드 구현

## 한줄 요약

현재 프로젝트는 백엔드 기본 구조, YOLO 탐지 래퍼, PPE 판정, 위험구역 침입 감지, JSONL 로그 저장, CCTV 데모 스크립트까지는 연결된 상태입니다.

다음 단계는 실제 CCTV형 MVP 완성을 위해 이벤트 중복 억제, 체류 시간 감지, 이벤트 조회 API, 최소 대시보드 구현으로 넘어가는 것입니다.
